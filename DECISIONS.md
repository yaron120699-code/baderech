# DECISIONS.md — בדרך. — יומן החלטות

קובץ זה מתעד שינויים משמעותיים שבוצעו על גבי הפרויקט הקיים (לא מוצר חדש). מבנה כל רשומה: **מה השתנה → למה → על איזה עיקרון/מחקר מבוסס → מה מצופה שישתפר**.

הערה חשובה: אין לי גישת push לריפו שלך ב-GitHub. השינויים בקובץ הזה בוצעו על עותק מקומי של הקוד (מ-`git clone` של הריפו הציבורי), ומסופקים לך כקובץ patch + הקבצים המעודכנים במלואם, כדי שתוכל להחיל אותם (`git apply baderech-changes.patch`, או פשוט להחליף את שלושת הקבצים ידנית) ולעשות את ה-commit בעצמך.

---

## 2026-07-30 — סבב שיפורים ראשון על ה-fit quiz הקיים

### קודם כל — ממצא חשוב מבדיקת הפרויקט הקיים
בדקתי את הריפו ואת האתר החי לפני שנגעתי בכל דבר. שני ממצאים חיוביים שכדאי שתדע:
1. **הברקוד כבר מפנה נכון.** פענחתי את קובץ ה-QR (`qr/baderech-fit-qr.png`) והוא מוביל ל-`https://baderech.vercel.app/fit?src=qr-card` — כלומר "הברקוד כשער כניסה למסע ולא לדף נחיתה" **כבר קיים במוצר**. לא נגעתי בזה כי הוא כבר מיושם נכון.
2. **ה-fit quiz כבר בנוי ברמה גבוהה יחסית** — 9 שאלות, ניקוד אמיתי (לא קוסמטי) ל-3 תוצאות, הודעת WhatsApp שמשתמשת בתשובות בפועל, שמירת state ב-sessionStorage, ניהול focus לנגישות, ותמיכה ב-`prefers-reduced-motion`. השיפורים למטה הם תוספות על בסיס טוב, לא תיקון של בעיה.

בגלל זה **ביטלתי את הפרוטוטייפ שבניתי בהודעה הקודמת** (רקע כהה + ברונזה) — הוא לא תאם את שפת המותג האמיתית (קרם חם + סאג', Frank Ruhl Libre + Heebo), שכבר עקבית ועובדת טוב. השינויים מכאן והלאה עובדים אך ורק בתוך מערכת הטוקנים הקיימת (`--bg`, `--sage`, `--sage-deep` וכו׳) — בלי צבע, גופן או טוקן חדש אחד.

### 1. מסך "תיאטרון עיבוד" חדש בין השאלון לתוצאה (`fit/index.html`, `fit/fit.js`, `fit/fit.css`)
**מה השתנה:** נוסף מסך ביניים (`data-step="matching"`) שמופיע לכ-2.4 שניות אחרי לחיצה על "לסיום", עם 3 נקודות נושמות (בשימוש חוזר ב-`.moment-dot` הקיים כבר ב-`css/style.css` — לא נוצר טוקן חדש) ומסר שמתחלף ("מחברים את מה שענית" → "מסתכלים על זה כמכלול" → "כמעט מוכן"), לפני שהתוצאה בפועל מוצגת.
**למה:** קודם התהליך היה: לחיצה על "לסיום" → קפיצה מיידית לתוצאה. זה גורם לתוצאה להרגיש כמו lookup table סטטי.
**מבוסס על:** מחקר Noom — "תיאטרון העיבוד" בין השאלון לתוצאה הוא לא קישוט, הוא מה שגורם לתוצאה להרגיש מחושבת ובעלת ערך, גם כשהחישוב עצמו מיידי (ראו מסמך המחקר הראשון, שלב 4).
**מצופה שישתפר:** תחושת "ערך" ורצינות ברגע קבלת התוצאה, בלי לשנות דבר בניקוד או בלוגיקה עצמה.
**סיכון/הערה:** קיצרתי ל-2.4 שניות (ולא 8-12 כמו ב-Noom) כדי לא להרגיש כמו עיכוב מלאכותי במוצר שכולו על כנות. אפשר להאריך מעט אם ירצה — זה קל לכיוונון (`setTimeout(..., 2400)` ב-`finishQuiz()`).

### 2. פס ההתקדמות: מ"X מתוך 9" לציוני דרך איכותיים (`fit/fit.js`)
**מה השתנה:** `progressLabel.textContent` כבר לא מציג ספירה מדויקת ("3 מתוך 9"), אלא ציון דרך מילולי ("מתחילים" / "בתנועה" / "בערך באמצע" / "כמעט שם" / "שאלה אחרונה"), מחושב לפי אחוז ההתקדמות בפועל.
**למה:** ספירה מדויקת מזמינה את המשתמש "לחשב" אם שווה להמשיך — זו בדיוק הבעיה שראינו במחקר על BetterHelp (שאלון של 30 שלבים, ותלונות מפורשות על אורך). כאן מדובר רק ב-9 שלבים (סביר לגמרי), אבל אין סיבה לא לאמץ את העיקרון גם כשהסיכון קטן יותר.
**מבוסס על:** מסמך האסטרטגיה, שלב 2 (קפסולות בלי מספרים) + ביקורת BetterHelp במסמך המחקר.
**מצופה שישתפר:** פחות "חישוב כדאיות" מודע, במיוחד בשאלות 3-4 (שהן הכי כבדות מבחינת תוכן: הטקסט הפתוח + סולם 1-5).
**הערה לשיקול דעתך:** זה שינוי קטן והפיך. אם אתה מעדיף את הוודאות של מספר מדויק (יש גם טיעון הפוך: מספר קונקרטי בונה אמון כי הוא לא מסתיר כמה נשאר) — קל לחזור אחורה, השורה הרלוונטית מסומנת בבירור בקוד.

### 3. מיקרו-אישור (Toast) בין שאלה לשאלה (`fit/index.html`, `fit/fit.js`, `fit/fit.css`)
**מה השתנה:** אחרי כל לחיצה על "המשך" מתוך שאלה (לא ממסך הפתיחה), מופיעה לרגע (1.3 שניות) הודעת טקסט קטנה בתחתית המסך — משפט אחד מתוך מאגר של 5 ("הבנו.", "רשמנו את זה.", "תודה על הכנות." וכו׳), שנבחר אקראית.
**למה:** זה בדיוק מה ש-Typeform עושה עם "תודה! עוד שאלה אחת" — יוצר תחושת דיאלוג עם מישהו שמקשיב, לא תחושת טופס שנשלח לריק.
**מבוסס על:** מסמך המחקר הראשון, פרק על Typeform ואפקט הדיאלוג.
**מצופה שישתפר:** תחושת "מישהו שם" לאורך השאלון, במיוחד בשאלות הרגישות יותר (Q1, Q6, Q8).

### 4. קישור משני "לקרוא קודם" במסכי תוצאה A ו-B (`fit/index.html`, `fit/fit.css`)
**מה השתנה:** תחת כפתור ה-WhatsApp הראשי בתוצאות "בסיס לשיחה" ו"שווה לדייק", נוסף קישור טקסט עדין (לא כפתור, לא מתחרה חזותית) שמפנה לעוגן רלוונטי בדף הבית (`/#process` ל-תוצאה A, `/#different` ל-תוצאה B).
**למה:** כרגע מי שמגיע מהברקוד עלול לעבור ישר לוואטסאפ בלי לראות אף מילה מדף הבית (סיפור, תהליך, גבולות, FAQ). זה עלול ליצור פנייה נכנסת עם ציפיות פחות מבוססות, גם עבורך וגם עבור הפונה.
**מבוסס על:** שיקול UX ישיר, לא ממצא ספציפי ממחקר — זו החלטה זהירה, לא אגרסיבית: הקישור לא מתחרה עם ה-CTA הראשי, הוא רק מוסיף אופציה.
**מצופה שישתפר:** פחות פניות "עיוורות" בוואטסאפ; מי שבאמת ממהר עדיין יכול ללחוץ ישר על הכפתור הראשי, שלא השתנה.
**⚠️ החלטה גדולה יותר שלא ביצעתי, ומעלה אליך לדיון:** השאלה העקרונית האם תוצאה A/B צריכות להוביל ישר לוואטסאפ (כמו היום), או לעבור קודם דרך דף הנחיתה כ"המשך טבעי" (כמו שתואר במסמך האסטרטגיה המקורי, שלבים 5-6) לפני הוואטסאפ. זה שינוי כיוון של כל הזרימה, ולכן לא ביצעתי אותו לבד — רק הוספתי אופציה עדינה. אם תרצה, אפשר לדבר על זה ולהחליט יחד.

---

## מה בדקתי ובחרתי *לא* לגעת בו (ולמה)

- **מבנה השאלות עצמן (Q1 עם עד 2 בחירות מתוך 5 אפשרויות).** במחקר ראינו ש-Headspace שומר על 2-3 אפשרויות בלבד לכל שאלה, כדי לצמצם עומס קוגניטיבי. Q1 כאן מציעה 5. זה שינוי בתוכן/בקול של השאלון (לא רק בממשק), ולכן לא נגעתי בו לבד — זו בדיוק סוג ההחלטה שכדאי שנדבר עליה, לא שאני אחליט.
- **קוד CSS "יתום" ב-`css/style.css`** (`.moments`, `.compass`, `.frames`, `.origin`, `.phil`, `.pride`, `.reflect__card` וכו׳) — נראה כמו שרידים מגרסה קודמת של דף הבית שאינם בשימוש ב-`index.html` הנוכחי. לא מחקתי כלום, כי אין לי איך לדעת אם זה מיועד לשימוש עתידי (במיוחד `.reflect__card`, שיש לו לוגיקת JS מלאה ב-`js/script.js` אבל אין לו HTML פעיל בשום דף — אולי זה בדיוק "מיני-שאלון בדף הבית עצמו" שתכננת בעבר ועדיין רלוונטי?). שווה שתבהיר לי אם זה dead code שאפשר לנקות, או פיצ'ר שממתין לחיבור.

---

---

## 2026-07-30 (המשך) — ניתוב תוצאות A/B דרך דף הנחיתה, לפי החלטתך

בשיחה שאלתי אם תוצאות A/B צריכות לעבור קודם דרך דף הנחיתה לפני וואטסאפ, או להישאר ישירות. **החלטת: לעבור קודם דרך דף הנחיתה.** יישמתי:

### 5. CTA ראשי בתוצאות A ו-B: כבר לא פותח וואטסאפ, אלא ממשיך לדף הנחיתה (`fit/index.html`, `fit/fit.js`, `fit/fit.css`, `js/script.js`)
**מה השתנה:**
- הכפתור הראשי בתוצאות A/B ("להמשיך אל הדרך") הוא כעת קישור רגיל (לא `target="_blank"`, לא `.fit__whatsapp-link`) שמנווט ל-`/#story` באתר הראשי — כלומר ממשיך לקרוא את הסיפור, התהליך, הגבולות וה-FAQ, ומגיע לוואטסאפ משם באופן טבעי (הקישורים הקיימים ב-Hero וב-Contact).
- נוסף קישור משני עדין ("אני מעדיף לדלג ישר לוואטסאפ") לכל מי שממש ממהר — לא נכפה על אף אחד לקרוא, רק זו כבר לא ברירת המחדל.
- **"גשר" חדש לשמירת ההקשר האישי:** כשמגיעים לתוצאה A/B, סיכום התשובות (הטקסט המורכב, לא התשובות הגולמיות) נשמר פעם אחת ב-`sessionStorage` (`baderech_wa_prefill`). ב-`js/script.js` של דף הנחיתה נוסף קוד קטן שבודק את המפתח הזה בטעינת הדף, ואם הוא קיים — מחליף את ה-href של כל קישורי הוואטסאפ הקיימים בדף (ב-Hero וב-Contact) כך שגם הם יישאו את הסיכום האישי, ואז מוחק את המפתח מיד (שימוש חד-פעמי). כך גם מי שבוחר לקרוא את כל הדף ורק בסוף ללחוץ על "וואטסאפ" — הפנייה שלו עדיין מגיעה עם הקשר, לא ריקה.
**למה:** כדי שדף הנחיתה יורגש כהמשך טבעי של מה שהמשתמש כבר ענה, לא כאיפוס לשיחה גנרית.
**מבוסס על:** ההחלטה שלך בשיחה + מסמך האסטרטגיה המקורי, שלב 5 ("המעבר לדף הנחיתה... איך גורמים לו להרגיש שזו המשכיות טבעית").
**מצופה שישתפר:** פחות פניות "לא-מוכוונות" בוואטסאפ; פנייה שמגיעה אחרי קריאת הדף כולו צפויה להיות בשלה יותר.
**מגבלה שכדאי שתדע:** ה-`sessionStorage` הוא per-tab. אם המשתמש עובר מ-`/fit` לדף הבית בלשונית חדשה (למשל דרך "פתח בלשונית חדשה"), הגשר לא יעבוד והוא יקבל את הודעת ברירת המחדל הרגילה. זה מקרה קצה נדיר, לא טיפלתי בו כרגע.
**עוגן שנבחר:** שלחתי את שתי התוצאות (A ו-B) לאותו עוגן (`/#story`) לפשטות. אם תרצה עוגנים שונים לכל תוצאה (למשל B ל-`/#who` כדי לחדד את שאלת ההתאמה) — זה שינוי של שורה אחת בכל מקרה, קל לכוונן.

---

## 2026-07-30 (סבב שלישי) — מ-Quiz Premium למסע: שבירת קצב שאלה-תשובה

אמרת בפירוש: העיצוב והקצב כבר מרגישים כמו חוויה, אבל המוח עדיין מזהה Quiz. ביקשת לשבור את המבנה שאלה→תשובה→שאלה, בלי לזרוק את מה שכבר בנוי. הנה מה שהשתנה, ולמה:

### 6. שלושה מסכי "רפלקציה" חדשים — בלי שום פעולה, רק מחשבה (`fit/index.html`, `fit/fit.js`, `fit/fit.css`)
**מה השתנה:** נוספו 3 מסכים חדשים (`reflect-1`, `reflect-2`, `reflect-3`) בתוך רצף השאלות — לפני Q1, לפני Q3, ולפני Q7. אין בהם שום קלט, שום כפתור בולט — רק שתי שורות טקסט (אחת רגילה, אחת מעומעמת/muted) ולחיצה בכל מקום במסך ממשיכה הלאה. שתיים מהן משתמשות ישירות בטקסטים שכתבת בהודעה שלך.
**למה:** זה בדיוק מה שביקשת — "מסכים שאין בהם שום פעולה, רק מחשבה". הם שוברים את הקצב החזוי של שאלה→תשובה, ונותנים למוח רגע לעכל בלי לדרוש ממנו להחליט משהו.
**מבוסס על:** הבקשה הישירה שלך + מחקר על "friction by design" בבריאות דיגיטלית (עיכוב מכוון ורפלקציה, לא כל דבר צריך להיות חסר-חיכוך) ומחקר על Arc Browser ("optimizing for feelings over data").

### 7. מסך "Pause" אחד — כמעט ריק, עם עיכוב מכוון (`fit/index.html`, `fit/fit.js`, `fit/fit.css`)
**מה השתנה:** נוסף מסך `pause-1` בין Q4 ל-Q5 (אחרי השאלה הפתוחה על מה שהיה משתנה) — רק 3 נקודות נושמות (אותו `.moment-dot` הקיים) ומילה אחת ("רגע."). הקישור להמשך לא מופיע מיד — הוא נבנה (opacity) רק אחרי 1.8 שניות, כדי לכפות רגע שקט אמיתי במקום המשכיות מיידית.
**למה:** זה ה"Pause" שביקשת במפורש בתרשים הזרימה החדש. הבחירה הספציפית של המיקום (אחרי Q4) היא כי זו השאלה הכי "פתוחה ורגשית" בשאלון (חודש וחצי קדימה) — נראה לי המקום הטבעי ביותר לנשימה.
**מגבלת נגישות שטופלה:** ב-`prefers-reduced-motion: reduce` העיכוב מבוטל לגמרי (הקישור מופיע מיד), כדי לא לפגוע במשתמשים שדורשים פחות תנועה/המתנה.

### 8. מיקרו-Insights מותאמי-תשובה, במקום אישורים גנריים אקראיים (`fit/fit.js`)
**מה השתנה:** ה-toast שמופיע אחרי כל שאלה כבר לא אקראי מתוך 5 משפטים כלליים — הוא מבוסס על **התשובה הספציפית** שנבחרה, לכל אחת מהשאלות q2, q4, q5 (סולם), q6, q7, q8 (ול-q1 יש הבחנה בין "רק סקרנות" לשאר). לדוגמה: מי שעונה "אני צריך שמישהו יוציא אותי מזה" (q6_3) מקבל "לא מעט אנשים מגיעים לכאן מתוך התחושה הזו בדיוק" — לא משפט גנרי.
**למה:** זה בדיוק ההבדל בין "המערכת מגיבה" ל"המערכת עונה תשובה אקראית שנשמעת אנושית". ביקשת תגובה שמרגישה אנושית אך לא מזויפת — לכן **לא הכנסתי אף אחוז או נתון סטטיסטי מומצא** (למשל לא "73% מהאנשים..."), רק ניסוחים איכותיים שיכולים להיאמר בכנות על ידי מישהו שבאמת הקשיב.
**מבוסס על:** הדוגמאות שנתת ("זו בחירה שמעט אנשים עושים", "זה דורש הרבה כנות") + עקרון האותנטיות שהדגשת ("לא בצורה מזויפת").

### 9. מסך התוצאה: משפה של "אבחון" לשפה של "מראה" (`fit/index.html`)
**מה השתנה:** הכותרת מעל כל תוצאה שונתה מ"התוצאה שלך" (מרגיש כמו ציון/פסק דין) ל"מה שעלה מתוך התשובות שלך". הטקסטים עצמם חוזקו בכיוון דומה ("זה לא אבחנה ולא ציון", "שני המקומות האלה לגיטימיים באותה מידה").
**למה:** בדיוק כמו שביקשת — לא "התאמה גבוהה" שנשמע כמו שיפוט אלגוריתמי, אלא שיקוף של מה שכבר היה שם.
**מבוסס על:** הדוגמאות שנתת ("מה שעלה מתוך התשובות שלך...", "בלי תחושת שיפוט").

### מה *לא* שיניתי, ולמה (הביקורתיות שביקשת)
שקלתי ברצינות את ההצעה שלך לשבור לגמרי את מבנה שאלה-תשובה (למשל למודל שיחה חופשי לגמרי, בלי שאלות מוגדרות). **החלטתי שלא לעשות זאת, ואני רוצה להסביר למה, כי ביקשת שאהיה בעל דעה:**

לשאלון הזה יש תפקיד תפקודי אמיתי מעבר לחוויה — הוא זה שמזין את מנגנון ההתאמה (A/B/C) ואת תוכן הודעת הוואטסאפ שמגיעה אליך עם הקשר אמיתי. חוויה "שיחתית" לגמרי בלי מבנה שאלה-תשובה ברור (למשל: קלט טקסט חופשי לגמרי לאורך כל הדרך, או ממשק מבוסס בחירת-מטאפורות) תדרוש ניתוח שפה טבעית (NLP/AI) כדי לחלץ ממנו מידע מובנה למיון ולניקוד — משהו שדורש קריאה ל-API חיצוני, עלות, וזמן תגובה שעלול לשבור בדיוק את תחושת ה"שיחה" שאתה רוצה (המתנה לתשובת AI). זה גם סיכון הנדסי גדול משמעותית מול ערך לא ודאי.

**מה שבחרתי לעשות במקום זה:** לשמור את עמוד השדרה של שאלה-תשובה (כי הוא ה"שלד" שעובד ומזין את המערכת), אבל **לפרק את הקצב החזוי שלו** — באמצעות המסכים החדשים שמפריעים לתבנית הצפויה, שינוי השפה מ"תוצאה נשפטת" ל"מראה", והמיקרו-Insights שהופכים כל תשובה לבעלת הד. אני חושב שזה משיג את מה שביקשת (שהמשתמש "ישכח שהוא עונה על שאלון") בלי לזרוק את התשתית שכבר עובדת, ובלי הסיכון של בנייה מחדש מלאה.

אם אחרי שתנסה את זה בעצמך תרגיש שזה עדיין לא מספיק רחוק מ"שאלון" — יש עוד כיוון אחד שלא מימשתי, ואשמח לדבר עליו: **להסתיר לגמרי את מבנה השאלה-תשובה הגלוי** (למשל, לא להראות תיבות סימון/רדיו כלל, אלא לתרגם כל שאלה לבחירה בין 2-3 "משפטים" שלמים שהמשתמש פשוט לוחץ עליהם כמו כרטיסיות טקסט גדולות בלי מסגרת של "טופס" — קרוב יותר לאיך ש-Duolingo או Arc מציגים בחירות כרגעים, לא כשדות קלט). זה שינוי עיצובי-ויזואלי משמעותי (לא רק CSS קטן) על גבי אותה תשתית לוגית בדיוק — לא שיניתי אותו כרגע כי זה שינוי גדול יותר בעבודה, ורציתי קודם שתראה ותאשר את הכיוון של הרפלקציה/Pause/Insights לפני שממשיכים לשם.

---

## הקבצים שהשתנו בפועל (מצטבר, שלושת הסבבים)
- `fit/index.html`
- `fit/fit.css`
- `fit/fit.js`
- `js/script.js`

---

## 2026-07-30 (סבב רביעי) — תיקון תקלה: מסך ה-Pause נראה כמו קפאון

דיווחת ש"משהו בflow נהרס". עברתי שוב על כל fit.js ו-fit.css שורה-שורה, ומצאתי חשוד מרכזי:

### 10. באג: רמז ההמשך במסך ה-Pause היה בלתי-נראה למשך 1.8 שניות, עם טכניקת CSS לא סטנדרטית (`fit/fit.css`)
**מה היה שבור:** מסך ה-Pause (בין Q4 ל-Q5) הציג את "להמשיך כשתהיה מוכן" עם `opacity:0` מוחלט למשך 1.8 שניות, ותלה את ההופעה שלו על אנימציית CSS שמנסה "לאנימץ" את התכונה `pointer-events` דרך keyframes — טכניקה לא סטנדרטית שלא כל דפדפן (בעיקר Safari במובייל, כלומר רוב מי שסורק את הברקוד מהאייפון) מיישם בעקביות. במקרה הגרוע: מי שמגיע למסך הזה רואה שלוש נקודות ומילה אחת ("רגע.") **בלי שום רמז חזותי** שיש דרך להמשיך — בדיוק תחושה של "האתר נתקע".
**התיקון:** הסרתי את כל טכניקת ה-pointer-events/keyframes. הרמז להמשיך עכשיו **תמיד גלוי** (מתחיל עמום ב-opacity 0.25, מתחזק ל-0.7 עם מעבר (transition) סטנדרטי פשוט של 1.4 שניות) — אף פעם לא נעלם לגמרי, ואף פעם אין "אזור מת" בלי אפשרות להמשיך.
**מבוסס על:** באג אמיתי שנמצא בבדיקת קוד, לא רק תיאוריה — הסרתי לגמרי טכניקה שידועה כבעייתית (animating a non-animatable/discrete property).
**מצופה שישתפר:** מי שמגיע למסך ה-Pause תמיד יראה משהו לחיץ, גם אם לוקח לו רגע לשים לב אליו.

**חשוב:** אם התקלה שחווית היא לא זו — תגיד לי בדיוק מה קרה (איזה מסך, מה לחצת, מה קרה בפועל לעומת מה שציפית) ואבדוק שוב ספציפית. אני לא יכול להריץ JS בדפדפן אמיתי מהצד שלי, רק לקרוא את הקוד — אז דיווח מדויק ממך הוא הדרך הכי מהירה לאתר עוד בעיות.

---

## 2026-07-30 (סבב חמישי) — Revert רעיוני: חזרה לפשטות

ביקשת revert רעיוני — לא למחוק קוד סתם, אלא להחזיר את החוויה למשהו פשוט, מדויק ואנושי. עשיתי קודם ניתוח של כל שלושת הקבצים (`fit/index.html`, `fit/fit.js`, `fit/fit.css`) ורק אז שכתבתי אותם.

### הניתוח שביצעתי לפני השינוי

- **מסכים מיותרים:** 3 מסכי רפלקציה + מסך Pause + מסך "תיאטרון עיבוד" — חמישה מסכים בלי שאלה אחת, על גבי 9 שאלות אמיתיות. זה יותר מסכי "אווירה" מאשר שאלות בפועל.
- **שאלות שחזרו על עצמן:** Q5 (סולם 1-5 "עד כמה אתה מוכן לנסות ולטעות") ו-Q8 ("האם אתה מוכן להשקיע") שאלו בעצם את אותה שאלת מוכנות בשני ניסוחים שונים. גם Q6 ("מי אחראי לשינוי") הרגיש יותר כמו שאלת פילוסופיה/אבחון מאשר שאלה שבאמת משנה את ההמלצה.
- **משפטים "כתובים מדי":** המיקרו-Insights ("זה דורש הרבה כנות", "לא מעט אנשים מגיעים לכאן מתוך התחושה הזו") היו תגובה אחרי **כל** שאלה, בניסוח ספרותי קבוע — זה בדיוק מה שהופך את זה לצפוי ומלאכותי אחרי 2-3 חזרות, גם אם כל משפט בפני עצמו היה בסדר.
- **איפה הקצב נתקע:** מסך ה-Pause (שכבר תוקן פעם אחת בגלל תקלה) היה בעצם הסימפטום של בעיה עמוקה יותר — יותר מדי "רגעי אווירה" נערמו זה על זה, וכל אחד מהם דחה את ההגעה לתוצאה.
- **איפה זה הרגיש כמו מבחן:** דווקא ה"אבחון הרך" (מסך עיבוד שמדמה ניתוח, ניסוחי "התאמה גבוהה/נמוכה") היה מה שיצר את תחושת ה"מבחן" — לא השאלות עצמן. ניסינו לרכך את זה עם שפה, אבל המבנה עצמו (עיבוד → תוצאה מדורגת) עדיין אמר "יש כאן מערכת ששופטת אותך".

### מה הוסר
- 3 מסכי רפלקציה → נשאר **אחד בלבד**, אחרי Q2 (במקום אחרי Q1, Q2 ו-Q6 בנפרד).
- מסך ה-Pause (עם ההשהיה של 1.8 שניות) — הוסר לגמרי.
- מסך ה"תיאטרון עיבוד" (matching) על כל האנימציה שלו — הוסר לגמרי. התוצאה מופיעה מיד אחרי השאלה האחרונה.
- מערכת המיקרו-Insights כולה (המיפוי לכל תשובה, ה-toast, הפונקציות `getInsight`/`showInsight`) — הוסרה לגמרי, לא רק צומצמה.
- שתי שאלות (הסולם "עד כמה מוכן לנסות ולטעות", ושאלת "מי אחראי לשינוי") — הוסרו כי חפפו שאלות אחרות.
- שאלת הטקסט הפתוח השנייה ("מה חשוב שאדע עליך לפני שנדבר") — הוסרה; המידע האיכותי היחיד שנשאר הוא Q3.
- קישור "לקרוא קודם" הנפרד מהכפתור הראשי — אין יותר צורך בו, כי הכפתור הראשי עצמו הוא עכשיו דף הבית.
- כל ה-CSS שתמך במסכים שהוסרו (scale, toast, matching, pause) — נמחק, לא רק "לא בשימוש".

### מה נשאר
- 6 שאלות בלבד: מה הביא אותך עכשיו (רב-ברירה), איפה אתה הכי תקוע, מה היית רוצה שישתנה (טקסט חופשי), תהליך אמיתי מול פתרון מהיר, פנים-מול-פנים, מוכנות להשקיע.
- מסך רפלקציה אחד, קצר: "אין כאן תשובה נכונה. רק ניסיון להבין אם זה מדויק לך עכשיו."
- מנגנון ניקוד פשוט יותר (רק 3 שאלות קובעות את התוצאה: תהליך-אמיתי, פנים-מול-פנים, מוכנות-להשקיע — לא כל 5 השאלות הישנות).
- 3 תוצאות בניסוח צנוע: "תוצאה מתאימה" / "תוצאה לא ודאית" / "תוצאה לא מתאימה כרגע" — בלי "התאמה גבוהה" ובלי "מה שעלה מתוך התשובות שלך".
- שמירת state ב-sessionStorage, נגישות (focus management, aria-disabled, prefers-reduced-motion), התאמה למובייל — כל אלה נשארו בדיוק כמו שהיו.

### מה השתנה ב-flow
**לפני:** QR → פתיחה → 9 שאלות עם 3 רפלקציות + Pause ביניהן → תיאטרון עיבוד (2.4 שנ') → תוצאה ("מה שעלה מתוך התשובות שלך") → CTA ראשי ל-`/#story` + קישור משני לוואטסאפ.

**אחרי:** QR → פתיחה קצרה → 2 שאלות → מסך רפלקציה אחד → 4 שאלות נוספות → תוצאה מיידית (בלי עיבוד) → CTA ראשי ל-`/` (ראש דף הבית) + קישור משני לוואטסאפ.

**מסלול המשתמש החדש, צעד-צעד:**
1. `intro` — משפט אחד + "מתחילים".
2. `q1` — מה הביא אותך עכשיו (רב-ברירה, עד 2).
3. `q2` — איפה אתה הכי תקוע.
4. `reflect-1` — מסך מעבר יחיד, בלי כפתור בולט, לחיצה בכל מקום ממשיכה.
5. `q3` — מה היית רוצה שישתנה (טקסט חופשי).
6. `q4` — תהליך אמיתי מול פתרון מהיר.
7. `q5` — פנים מול פנים.
8. `q6` — מוכנות להשקיע (כפתור "לסיום").
9. תוצאה מיידית (A/B/C), בלי מסך ביניים.
10. CTA ראשי → `/` (ראש דף הבית, לא `/#story`). קישור משני קטן → וואטסאפ ישירות, עם סיכום התשובות שנשמר גם עבור כפתורי הוואטסאפ הרגילים של דף הבית (אותו מנגנון גשר כמו קודם, פשוט הורחב לשלוש התוצאות ולא רק לשתיים).

### אילו קבצים עודכנו
- `fit/index.html` — נכתב מחדש (280 → 208 שורות).
- `fit/fit.js` — נכתב מחדש (514 → 383 שורות; ההשוואה הרלוונטית היא מול הגרסה המקורית של 358 שורות — כמעט חזרנו לאותו סדר גודל, עם ניקוד וטקסט וואטסאפ מעודכנים ל-6 שאלות).
- `fit/fit.css` — נכתב מחדש (270 → 192 שורות; כל ה-CSS שתמך במה שהוסר נמחק, לא רק "כובה").
- `js/script.js`, `css/style.css`, `index.html` הראשי — **לא נגעתי בהם בסבב הזה** (מנגנון הגשר ב-`js/script.js` נשאר זהה ותקף).

---

## 2026-07-30 (סבב שישי) — הגדלת טקסט כללית באתר

שאלת אם כדאי להגדיל את גודל הטקסט באופן כללי. הנה התשובה שלי וההחלטה:

### דעתי, לפני שביצעתי
הגדלה כללית היא בקשה סבירה ולא רק "עוד עדכון קוסמטי" — קהל היעד (גברים, כנראה טווח גילאים בוגר) נהנה מטקסט קריא בלי מאמץ, במיוחד בטלפון. הסיכון היחיד הוא שאם מגדילים רק כותרת אחת או שניים בלי מערכת אחידה, המרווחים (padding, line-height, מרחקים בין אלמנטים) מפסיקים "לדבר" עם גודל הטקסט החדש והעיצוב מתחיל להרגיש צפוף או לא מכוון. **לכן ביצעתי הגדלה שיטתית של כל 91 הגדרות ה-font-size בשני קבצי ה-CSS (`css/style.css` ו-`fit/fit.css`) יחד, לא רק כמה מקומות בררנית** — כך שהיחסים בין כותרת לתת-כותרת לטקסט רגיל נשארים זהים, רק הכל גדל יחד.

### מה בפועל השתנה (`css/style.css`, `fit/fit.css`)
**מה השתנה:** כל ערך font-size (גם ה-px הקבועים וגם כל שלושת הפרמטרים בכל clamp()) הוגדל פי 1.08 (כ-8%), מעוגל ל-0.5px הקרוב לשמירה על עקביות עם המערכת הקיימת (שכבר השתמשה בערכים כמו 15.5px, 16.5px). לדוגמה: טקסט גוף רגיל 16px → 17.5px, כותרת ה-Hero clamp(34px,6.2vw,72px) → clamp(36.5px,6.7vw,78px).
**מה לא נגעתי בו:** line-height (נשאר 1.75 לגוף הטקסט), מרווחים (padding/margin), רדיוס הפינות, וכל שאר הטוקנים — רק font-size.
**למה 8% ולא יותר/פחות:** זו הגדלה שמורגשת בבירור בקריאה (במיוחד בפסקאות ארוכות בדף הבית ובתוך שאלון ה-fit), אבל לא כזו שגורמת לכותרות ה-Hero (clamp עד 78px) להיראות מוגזמות או לשבור שורות בטלפון קטן.
**מבוסס על:** בקשתך + עקרון UX בסיסי — קריאות משפיעה ישירות על שיעור השלמה, במיוחד בתהליך שכולו טקסט (שאלון ה-fit ודף הבית).
**מצופה שישתפר:** פחות מאמץ בקריאה, במיוחד במובייל; לא אמור לשנות שום דבר במבנה, בצבעים, או בזרימה.

**זה קל לכוונן:** אם 8% ירגיש חזק מדי או חלש מדי אחרי שתראה את זה בפועל — זה שינוי של מספר אחד (`FACTOR`) בסקריפט שהרצתי, לא עריכה ידנית של 91 מקומות. תגיד לי כיוון ואני ארוץ שוב.

---

## 2026-07-30 (סבב שביעי) — נקודת שליטה יחידה לגודל טקסט (מספר אחד, לא סקריפט)

ביקשת דרך לשלוט בגודל הטקסט של כל האתר על ידי הקשת מספר אחד בקוד, בלי שתצטרך לבקש ממני להריץ סקריפט בכל פעם.

### מה השתנה (`css/style.css`, `fit/fit.css`)
**מה השתנה:** כל 91 הגדרות ה-font-size (כולל כל שלושת הפרמטרים בכל clamp()) הומרו מיחידות px קבועות ליחידות **rem** — יחידה שהיא תמיד יחסית לגודל הטקסט שמוגדר על אלמנט ה-`html` הראשי. בראש `css/style.css` נוספה נקודת שליטה יחידה, עם הסבר בעברית ממש שם:
```css
html{
  font-size: 16px; /* ← שנה רק את המספר הזה כדי להגדיל/להקטין את כל הטקסט באתר */
}
```
**איך זה עובד בפועל:** כל טקסט באתר (כותרות, גוף, כפתורים, שאלון ה-fit) מוגדר כשבר של אותו מספר בסיס. אם תשנה את ה-`16px` ל-`17px`, כל הטקסט באתר יגדל יחד ביחס זהה (כ-6% יותר), בלי לגעת בשום דבר אחר — לא במרווחים, לא בפריסה, לא בצבעים.
**חשוב לדעת:** ה-16px הנוכחי כבר כולל בתוכו את ההגדלה של 8% שביצעתי בסבב הקודם (כל ה-rem-ים חושבו מהערכים המוגדלים, לא מהמקוריים). כלומר: 16px = "המצב הנוכחי שכבר ראית", לא "המצב המקורי לפני שהגדלנו". אם תרצה לחזור אחורה מתחת לזה, תוריד את המספר; אם תרצה עוד יותר גדול, תעלה אותו.
**מבוסס על:** בקשתך המפורשת לשליטה ידנית פשוטה, בלי תלות בי להריץ סקריפט בכל פעם.
**מצופה שישתפר:** יכולת שלך לכוונן את גודל הטקסט של כל האתר בעצמך, מתי שתרצה, בשינוי מספר אחד.

---

## 2026-07-30 (סבב שמיני) — הפיכת בדיקת ההתאמה מ"שער כניסה" ל"שלב הבא"

ביקשת לשנות רק את המיקום של הפאנל במסע, לא את השאלות עצמן. הנה מה שהשתנה:

### 11. הסרת בדיקת ההתאמה מה-Hero (`index.html`)
**מה השתנה:** הקישור "בדיקת התאמה אישית — כשתי דקות" הוסר משורת ה-CTA הראשונה בעמוד (ה-Hero), לצד קלנדלי ווואטסאפ. עכשיו ה-Hero מציע רק שתי אפשרויות ליצירת קשר ישיר, בלי לדחוף את הפאנל לפני שנבנה אמון.
**למה:** זה בדיוק ה"שער כניסה" שרצית לבטל — הצעת בדיקת ההתאמה כאופציה שווה-ערך כבר בשנייה הראשונה, לפני שהמשתמש קרא מילה על התהליך.

### 12. בדיקת ההתאמה הפכה ל-CTA הראשי בסוף העמוד (`index.html`)
**מה השתנה:** במקטע הסיום (`#contact`), אחרי שכל התוכן הבונה-אמון כבר נקרא (סיפור, איך זה שונה, איך זה עובד, למי זה מתאים, שאלות נפוצות) — הכפתור הבולט ביותר (`.btn`) הוא עכשיו **"בדיקת התאמה קצרה — הצעד הבא"**, לא קלנדלי. קלנדלי, וואטסאפ ואינסטגרם עברו לשורת הקישורים המשניים מתחתיו.
**למה:** זה בדיוק העיקרון שביקשת — האתר בונה את האמון (כל מה שמעליו), והפאנל הוא הצעד הבא שמעמיק אותו, לא נתיב מקביל.

### 13. תוצאות הפאנל כבר לא חוזרות לדף הבית (`fit/index.html`, `fit/fit.css`)
**מה השתנה:** ה-CTA הראשי בתוצאות A ו-B היה "להמשיך ולהכיר את הדרך" → `/`. עכשיו, מכיוון שמגיעים לפאנל רק **אחרי** שכבר קראו את האתר, חזרה לדף הבית מיותרת ומעגלית. הכפתור הראשי הוא עכשיו **וואטסאפ ישירות** (עם סיכום התשובות), וקישור משני עדין מציע לקבוע שיחה בקלנדלי ישירות. תוצאה C (לא מתאים כרגע) חזרה למבנה הפשוט שהיה לה במקור — קריאה עדינה אחת בלבד לוואטסאפ, בלי שום קישור לדף הבית.
**למה:** "הפאנל מעמיק את האמון" — כשמישהו סיים גם לקרוא את האתר וגם למלא את הפאנל, השלב הבא ההגיוני הוא שיחה, לא מסך נוסף.
**מבוסס על:** ההנחיה שלך + עקרון פשוט — אל תשלח אף אחד למקום שהוא כבר ביקר בו.
**חשוב:** לא נגעתי ב-`fit/fit.js` בכלל בסבב הזה. השאלות, הניקוד, וכל הלוגיקה זהים ב-100% למה שהיה. השינוי הוא רק בסימון ה-HTML של הכפתורים (איזה קישור פתוח, לאן) — הקוד הקיים כבר תמך בזה (`fit.js` מזהה כל אלמנט עם class `fit__whatsapp-link` ומעדכן את ה-href שלו אוטומטית).

### 14. נקודה שלא נגעתי בה, ומעלה לדיון: הברקוד עצמו
**המצב הנוכחי:** הברקוד הפיזי (על כרטיס הביקור/A6) עדיין מקודד ומפנה ישירות ל-`/fit?src=qr-card` — כלומר, מי שסורק אותו עדיין מדלג ישר לפאנל, **בלי** לעבור דרך האתר קודם. זה כרגע לא עקבי עם הכיוון החדש (אתר בונה אמון → פאנל מעמיק).
**למה לא שיניתי את זה בעצמי:** אם כרטיסי ה-QR כבר הודפסו וחולקו בפועל (הריפו מכיל קבצי הדפסה מוכנים — `card-a6-105x148mm.png`, `card-business-85x55mm.png`), שינוי הכתובת המקודדת בברקוד יגרום לכל כרטיס שכבר בשטח להפנות למקום הלא-נכון (אין דרך "לעדכן" ברקוד שכבר הודפס על נייר). זו החלטה עם השלכה פיזית אמיתית, לא רק שינוי קוד — ולכן עצרתי ולא שיניתי את זה לבד.
**מה שכדאי שתחליט:** אם הכרטיסים עדיין לא הודפסו/חולקו בפועל — הכי נכון יהיה לשנות את הברקוד להפנות ל-`/` (דף הבית) במקום ל-`/fit`, כדי שהעיקרון החדש יהיה עקבי גם בכניסה הפיזית. אם כבר חולקו — אפשר להשאיר את הברקוד כמו שהוא (מי שסורק אותו עדיין יגיע ישר לפאנל, פשוט בלי המסלול המלא של בניית האמון קודם), או לשקול שהפאנל עצמו יציג משהו שונה למי שמגיע דרך `src=qr-card` (יש כבר פרמטר כזה ב-URL) — זה שינוי גדול יותר, ולא ביצעתי אותו בלי לדבר איתך קודם.

---

## 2026-07-30 (סבב תשיעי) — נוכחות אנושית ב-Hero: תמונה אחת, ושינוי הברקוד

### 15. תמונה אחת, א-סימטרית, בפינת ה-Hero (`index.html`, `css/style.css`, `images/yaron-hero.jpg`)
**מה השתנה:** נוספה תמונה יחידה (אתה עם הכלבה במרפסת) לתוך ה-Hero הקיים — בלי לשנות שורה אחת מהטקסט הקיים, ההיררכיה, או מבנה העמוד. התמונה ממוקמת בפינה השמאלית-תחתונה, מוצנעת, לא נוגעת בעמודת הטקסט המרכזית.

**למה המיקום הזה:** בעברית (RTL) הקריאה מתחילה מימין-למעלה. אם התמונה הייתה שם, היא הייתה מתחרה עם הטקסט על תשומת הלב הראשונה. פינה שמאלית-תחתונה נמצאת מחוץ למסלול הקריאה הטבעי, אבל עדיין לגמרי בתוך שדה הראייה — בדיוק כמו תמונה שמונחת על שולחן ליד מי שמדבר איתך, לא תלויה על הקיר מולך.

**למה החיתוך הזה:** במקום להשתמש בתמונה כפי שהיא (1600px גובה, המון שמיים ורצפה ריקים), חתכתי ליחס 4:5 שמשאיר פרוסה מכוונת של שמיים (רוגע, אור) ורצפה מעטה (הארקה) — אבל בלי המרחב הריק העודף. זה בדיוק העיקרון של "Editorial crop" — לא התמונה כולה, רק הרגע שבה.

**למה הגודל הצנוע:** clamp שנע בין 120-210px ברוחב (תלוי מסך) — קטן בכוונה. תמונה גדולה הייתה הופכת את זה ל"Banner" בדיוק כמו שביקשת שלא יקרה. הגודל הזה מספיק כדי שתבחין בבירור שיש שם בן אדם אמיתי, אבל לא מספיק כדי להתחרות עם הטקסט על תשומת לב.

**האנימציה היחידה:** fade+rise עדין מאוד (1.6 שניות, 10px בלבד) בטעינת הדף — לא scroll-triggered (התמונה כבר בתוך המסך הראשון, אין צורך), בלי פרלקס, בלי שום תזוזה אחרי הכניסה הראשונית. מכבד `prefers-reduced-motion` (מבוטל לגמרי אם המשתמש ביקש פחות תנועה).

**⚠️ באג אמיתי שנמצא ותוקן לפני שהראיתי לך את זה:** בדקתי את זה בפועל (רינדור בדפדפן אמיתי, לא רק בעיניים) על כמה גדלי מסך, ומצאתי שבמסכים נמוכים במיוחד (כמו iPhone SE, 375×667) התמונה **חפפה** גם את שורת הקישורים וגם את "לגלול הלאה". התיקון: במסכים כאלה התמונה פשוט לא מוצגת בכלל, במקום להידחס ולגרום לחפיפה. עדיף בלי תמונה מאשר עם תמונה שפוגעת בקריאות.

### 16. עדכון קובצי ה-QR בפועל (`qr/baderech-fit-qr.png`, `qr/baderech-fit-qr.svg`, `baderech-fit-qr.png`)
**מה השתנה:** שלושת קובצי ה-QR הגולמיים בריפו עודכנו להצביע על `https://baderech.vercel.app/?src=qr-card` (דף הבית) במקום `/fit?src=qr-card`. בדקתי בעצמי (פענוח QR בפועל) ששלושתם קוראים נכון.
**למה עכשיו:** אישרת שהכרטיסים הפיזיים עדיין לא הודפסו/חולקו — אז זה הרגע הבטוח לעשות את זה, לפני שיש עלות פיזית הפיכה.
**⚠️ מה עדיין לא טיפלתי בו:** `card-a6-105x148mm.png` ו-`card-business-85x55mm.png` — עיצובי כרטיסים מוכנים להדפסה שכנראה מכילים את ה-QR **הישן** כבר משוקע בתוך הקומפוזיציה הסופית. לא נגעתי בהם כי אני לא יודע בוודאות איך הם נבנו (Canva/Figma/AI) ואיזה סיכון יש בעריכה עיוורת שלהם. אלה עדיין צריכים עדכון ידני לפני הדפסה בפועל.

---

## 2026-07-30 (סבב עשירי) — תיקון: מסך קבלת פנים נפרד, לא תמונה בתוך ה-Hero

ציינת שלא הבנתי נכון — התכוונת שהתמונה תהיה על **מסך נפרד לפני** דף הנחיתה (הקבלת פנים עצמה), לא כקישוט בתוך ה-Hero הקיים. תיקנתי:

### 17. הסרת התמונה מה-Hero (`index.html`, `css/style.css`)
בוטל לגמרי — `.hero__photo` והאנימציה שלה נמחקו, ה-Hero חזר בדיוק למה שהיה לפני הסבב הקודם.

### 18. מסך קבלת פנים חדש, לפני האתר עצמו (`index.html`, `css/style.css`, `js/script.js`)
**מה נבנה:** overlay במסך מלא (`#greeting`) שמופיע **לפני** תוכן האתר, לא כ"עמוד" בפני עצמו עם URL נפרד — פשוט השכבה הראשונה שרואים בכניסה. בתוכו: התמונה שלך עם הכלבה (הפעם גדולה ומרכזית, כי כל התפקיד של המסך הזה הוא נוכחות — אין כאן טקסט שיווקי שמתחרה איתה), ומתחתיה חתימה שקטה "— ירון פסי". לחיצה/הקשה בכל מקום על המסך (או Enter/Space למקלדת) ממשיכה וחושפת את דף הבית מתחתיו.
**קצב:** התמונה נכנסת ראשונה (fade+rise עדין, 1.4 שנ'), החתימה אחריה (1.1 שנ' אחר כך), ורמז "להמשיך" עדין מופיע רק אחרי כ-2.6 שניות של שקט — לא מיד, כדי לתת רגע להסתכל לפני שמזמינים לפעולה.
**פעם אחת לכל session:** נשמר דגל ב-`sessionStorage` — מי שכבר ראה את זה (ואפילו רק רענן את הדף) לא יראה אותו שוב באותה ביקור. סקריפט inline קטן רץ **לפני** הרינדור הראשון של הדף, כך שלמי שכבר ראה את זה אין אפילו הבהוב-של-רגע לפני שהוא נעלם — הוא פשוט לא מוצג.
**נגישות:** ה-overlay מקבל focus (tabindex), אפשר להפעיל אותו גם עם Enter/Space, יש aria-label, ומכבד `prefers-reduced-motion` (הכל מופיע מיד בלי אנימציה אם המשתמש ביקש פחות תנועה). כשה-overlay פעיל, גלילה של שאר הדף חסומה (`overflow:hidden`), כדי שלא יהיה אפשר לגלול "מתחת" אליו בטעות.
**בדיקה בפועל:** רינדרתי את זה בדפדפן אמיתי (לא רק עיניים) על כמה גדלי מסך, ובדקתי פרוגרמטית: התמונה ממורכזת נכון, אין חפיפות, ה-session-persistence עובד (רענון/כניסה חוזרת לא מציגים את זה שוב), והמעבר לחשיפת ה-Hero אחרי לחיצה עובד חלק.

---

## 2026-07-30 (סבב אחד-עשר) — ניקוי קווים דקורטיביים בכל האתר

עברתי על כל `css/style.css` ו-`fit/fit.css` וחיפשתי כל `border`, `::before`/`::after`, `height:1px` וכל מבנה divider. בדקתי כל אחד לפי המבחן שנתת: "אם הקו לא מסביר, לא מפריד באמת ולא מאפשר פעולה — הוא לא צריך להיות שם."

### מה הוסר (5 מקומות, כולם בקובץ `css/style.css`)

1. **`.eyebrow::before`** — הקו הקצר שהופיע לפני כל "eyebrow" באתר (בדיוק הדוגמה שנתת: "תהליך אישי אחד על אחד ───"). זה משפיע על **כל** ה-eyebrow-ים באתר בבת אחת — ב-Hero, Identification, Story, Different, Process, Who, FAQ, Contact. גם ניקיתי את ה-`display:inline-flex`/`gap` שהיו קיימים רק כדי ליישר מול הקו שכבר לא קיים.
2. **`.id__close`** (מקטע Identification) — הוסר `border-top` לפני משפט הסיום ("לא חייבים לדעת עדיין לאן הולכים..."). ה-`margin-top:46px` שכבר היה שם מספיק ליצור הפרדה.
3. **`.real__photo`** (מקטע "אז/היום") — הוסרה המסגרת הדקה סביב שתי התמונות. עיגול הפינות (`border-radius:16px`) לבד כבר מגדיר את התמונה כאובייקט ברור.
4. **`.different__line`** (4 המשפטים במקטע "אני לא שולח אותך להתמודד לבד...") — הוסרו הקווים בין כל שורה. **הוחלף** ב-`gap:20px` (במקום 2px) — הפרדה עכשיו נוצרת ממרווח לבן, לא מקו.
5. **`.process__item`** (4 השלבים הממוספרים 01-04) — הוסרו הקווים בין השלבים. **הוחלף** ב-`gap:14px` בין הפריטים + מרווח פנימי מעט גדול יותר. המספור עצמו (01/02/03/04) כבר יוצר את המבנה — הקו היה שכבה מיותרת מעליו.

### מה נשאר בכוונה (ונבדק שהוא פונקציונלי, לא קישוט)

- **`.nav{border-bottom}`** — ה-navbar קבוע (fixed) עם רקע מטושטש; הקו מפריד אותו בפועל מהתוכן שגולל מתחתיו.
- **`.textlink`, `.contact__links a`** — קווי underline על קישורים אמיתיים (הם *האינדיקציה* שזה קישוט לחיצה, לא קישוט חזותי — יש להם גם שינוי צבע ב-hover).
- **`.fit__option`, `.fit__field textarea`, `.fit__btn-secondary`, `.nav__cta`, `.btn`** — כל הגבולות בשאלון ה-fit ובכפתורים הם גבולות של רכיבים אינטראקטיביים אמיתיים (שדות, כפתורים, תיבות בחירה).
- **`.boundaries`, `details.faq-item`, `.fit__card`** — אלה "כרטיסים" אמיתיים (background+blur+border) שמקבצים תוכן קשור יחד — לא קו דק, אלא מסגרת שלמה שמגדירה אזור תוכן.
- **`.fit__progress-track`** — פס ההתקדמות בשאלון. יש לו מצב אמיתי (אחוז מילוי) — זו לא קישוט, זה UI.
- **`.who__fit li::before{content:"←"}`** — לא קו, תו טקסטואלי (חץ) שמחליף נקודת רשימה רגילה ומעביר משמעות (כיוון/דרך) — נשאר.
- **`details.faq-item summary::after`** — סימן ה-"+" שהופך ל-"×" בפתיחה/סגירה של כל שאלה — אינדיקציית מצב אמיתית.

### מה לא נגעתי בו, ולמה

ישנם בלוקים שלמים של CSS (`.frames`, `.phil`, `.build`/`.compass`, `.origin`, `.areas__close`, `.goal`, `.pride`) עם קווי הפרדה משלהם — **אבל בדקתי ואישרתי ששום אחד מהם לא מופיע בפועל ב-`index.html` הנוכחי**. אלה שרידי קוד מגרסה קודמת של הדף (כבר סימנתי את זה בעבר). לא ניקיתי אותם כחלק מהמשימה הזו כי המשימה הייתה ניקוי קווים **באתר החי**, לא בקוד מת — אבל אם תרצה שאמחק גם את הבלוקים המתים האלה לגמרי (לא רק את הקווים בתוכם), זו משימה קטנה נפרדת.

### מסך קבלת הפנים (ירון והכלבה)
לא נגעתי בו כלל בסבב הזה — `.greeting`, `.greeting__photo`, `.greeting__sig` משתמשים ב-`box-shadow` עדין, לא ב-`border`, ונשארו בדיוק כמו שהיו.

**קובץ שנבדק ולא נדרש בו שינוי:** `fit/fit.css` — עברתי על כל ה-border-ים שם ומצאתי שכולם פונקציונליים (כרטיס השאלון, שדות קלט, כפתורים, פס התקדמות, אינדיקציית radio/checkbox) — אין שם אף קו דקורטיבי להסיר.

---

## 2026-07-30 (סבב שתיים-עשרה) — עדכון README

**מה השתנה:** `README.md` נכתב מחדש. הגרסה הקודמת תיארה רק דף נחיתה סטטי פשוט — לא הזכירה בכלל את `/fit`, את מסך קבלת הפנים, את תיקיית ה-QR, או את `DECISIONS.md`. גם היה בו סעיף לא-מדויק על "טופס יצירת קשר mailto" שכבר לא קיים באתר.
**מה נוסף:** תרשים המסע המלא (QR → קבלת פנים → אתר → בדיקת התאמה → תוצאה → וואטסאפ/קלנדלי), מבנה עדכני של כל התיקיות כולל `fit/`, `qr/`, `images/yaron-hero.jpg`, והפניה ל-`DECISIONS.md` כמקור האמת להיסטוריית ההחלטות.
**הערה טכנית שנוספה, וחשובה בפועל:** הסברתי במפורש שפתיחת `fit/index.html` כקובץ (`file://`) לא תעבוד כי הוא טוען CSS/JS בנתיבים מוחלטים — צריך שרת מקומי. זו לא הערה תיאורטית — זו בדיוק הבעיה שגרמה לי לבנות שרת מקומי לבדיקות באמצע השיחה הזו.

---

## 2026-07-30 (סבב שלושה-עשר) — תיקוני ה-Product Audit

ביצעתי Product Audit מלא של המסע (QR → וואטסאפ), ואז את התיקונים שעלו ממנו.

### 19. תיקון ה-Hero — הסרת קישורי יצירת קשר ישירים (`index.html`)
**מה השתנה:** שני הקישורים (קלנדלי + וואטסאפ) שהיו ב-Hero הוסרו לגמרי. עכשיו ה-Hero מציג רק כותרת, תת-כותרת, ורמז גלילה.
**למה:** זו הייתה הסתירה האמיתית היחידה שעלתה באודיט — לאחר שכבר הוצאנו את בדיקת ההתאמה מה-Hero (כדי שלא תהיה "שער" לפני בניית אמון), נשארו שם דווקא שתי רמות המחויבות הגבוהות ביותר באתר. זה סתר ישירות את העיקרון "האתר בונה אמון, הפאנל מעמיק אותו".
**לא נגעתי:** ב-`.hero__actions` ב-CSS (עדיין קיים, לא בשימוש) — לא מחקתי כי זה שינוי קוסמטי-נקיון בלבד, לא פונקציונלי.

### 20. רמז עדין על רמת ההשקעה (`index.html`, מקטע `#who`)
**מה השתנה:** נוסף בולט חמישי לרשימת "למי זה מתאים": "מבין שזה תהליך שדורש השקעה משמעותית — בזמן, בנוכחות, ולפעמים גם במשאבים."
**למה:** זו הייתה אחת משתי השאלות הפתוחות באודיט. בחרת לרמוז (לא לפרט מספר), ומיקמתי את זה בתוך רשימת ההתאמה-העצמית הקיימת — כי זה בדיוק המקום שכבר עושה סינון-עצמי, לא מקום חדש.

### 21. טיפים חינמיים בתוצאה C ("לא מתאים כרגע") — `fit/index.html`, `fit/fit.css`
**מה השתנה:** מי שמגיע לתוצאה "לא מתאים כרגע" מקבל עכשיו גם 3 משפטים קצרים (לא רשימה עם כותרות, לא "טיפים" מתויגים במפורש — רק שלוש מחשבות בגוף הטקסט), לפני קישור ה"בכל זאת לשאול" הקיים.
**למה זה נבחר במקום החלופות:** ביקשת "כמה טיפים חינמיים כהתחלה" — ונקודה חשובה: זה בדיוק פוגע בול בסגמנט הזה. תוצאה C מגיעה בעיקר ממי שענה שהוא מחפש "פתרון מהיר או כמה טיפים" (Q4) ו/או "משהו חינמי או נקודתי" (Q6) — כלומר, נתינת טיפים בדיוק כאן היא לא רק "מתנת פרידה" אלא מתן בדיוק מה שהסגמנט הזה ביקש, בלי שהוא יצטרך לשאול.
**⚠️ חשוב מאוד:** הניסוח של שלושת הטיפים הוא **טיוטה שלי**, לא תוכן סופי. אני לא מכיר את השיטה או הניסיון המקצועי שלך לעומק כמו שאתה מכיר, ולכן כתבתי טיפים כלליים וזהירים שמתאימים לטון של האתר — אבל אלה בדיוק המקום שבו כדאי לך להחליף לתוכן אמיתי שלך, לא להשאיר את הניסוח שלי כמו שהוא.
**מבוסס על:** הבחירה שלך בשיחה + הזיהוי שזה פותר בו-זמנית גם את ה-Missing Moment (מי שיוצא מהפאנל בלי כלום) וגם משרת את הסגמנט שממילא ביקש בדיוק את זה.

---

## 2026-07-30 (סבב ארבע-עשרה) — שאלה 3: מטקסט חופשי לבחירה מרובה + תווית "צידה לדרך"

### 22. שאלה 3 הפכה ל"שאלה אמריקאית" (`fit/index.html`, `fit/fit.js`, `fit/fit.css`)
**מה השתנה:** "אם משהו אחד היה משתנה — מה היית רוצה שייראה אחרת?" עברה מ-textarea חופשי ל-5 אפשרויות בחירה (כולל "קשה לי לבחור אחת — זה קצת מהכול", כדי לא לאבד את מי שבאמת לא בטוח). הוסר גם ה-CSS של שדה הטקסט (`.fit__field`, `.fit__counter`) שכבר לא בשימוש.
**למה:** הסכמת שהקלדה חופשית באמצע שאלון קצר, במובייל, היא בדיוק סוג הדבר שיכול לגרום לנשירה — במיוחד ששאלה 3 היא כבר השאלה החמישית-בפועל (אחרי Q1, Q2, ומסך הרפלקציה) שבה המומנטום עוד לא לגמרי מבוסס.
**עדכון טכני נלווה:** `buildWhatsAppMessage()` ב-`fit.js` השתמש בעבר בטקסט הגולמי של q3; עכשיו הוא משתמש ב-`label('q3', ...)` בדיוק כמו כל שאר השאלות, כי אין יותר טקסט חופשי להציג.

### 23. תווית "צידה לדרך, ממני אליך" מעל הטיפים בתוצאה C (`fit/index.html`, `fit/fit.css`)
**מה השתנה:** נוספה תווית קטנה מעל שלושת הטיפים, והוסר משפט כפול שהיה בלד ("אבל בא לי להשאיר לך משהו") כי הוא אמר בדיוק את אותו דבר כמו התווית החדשה.
**ממתין:** הטיפים עצמם עדיין הטיוטה שלי — מחכה לתוכן האמיתי שלך כדי להחליף.

---

## 2026-07-30 (סבב חמש-עשרה) — הטיפים האמיתיים בתוצאה C

**מה השתנה:** שלושת הטיפים שלי הוחלפו לגמרי בתוכן האמיתי שנתת — כותרת ("שלושה דברים שכולם יודעים — אבל לא תמיד באמת עושים."), ומבנה דו-שכבתי לכל טיפ: תובנה (איטלי, מעומעם, כמו קודם) + פעולה קונקרטית ("בפעם הבאה: ...", בצבע sage-deep, פחות מעומעם — כדי שהפעולה תבלוט יותר מהתובנה).
**עיקרון שציינת ואני רושם לעצמי להמשך:** אתה מתכנן מאגר גדול יותר של עקרונות, ומכל פעם להציג רק שלושה — לא כדי שהדף יהפוך למדריך ארוך. כרגע יש בדיוק שלושה, אז אין עדיין צורך במנגנון בחירה אקראית — אבל כשיתווספו עוד, יהיה קל להוסיף בחירת-שלושה-אקראית מתוך מאגר גדול יותר (שינוי קטן ב-`fit.js`, לא מבני).

---

## 2026-07-30 (סבב שש-עשרה) — מקטע "איך נראית הדרך?" (ארבע התנועות)

### 24. הוספת מקטע חדש בין "מה שונה" ל"איך זה עובד" (`index.html`, `css/style.css`)
**מה נוסף:** מקטע קצר `#movements` עם ארבע התנועות (לראות / לבחור / לפעול / להחזיק), משפט קצר אחד לכל תנועה, ומשפט סיום ("אנחנו לא מנסים להביא את כולם לאותו מקום...").
**מיקום:** בין `#different` ל-`#process` — הזרימה: קודם *למה זה שונה*, אז *תמונת-העל של ארבע התנועות*, ורק אחר כך *הפירוט המעשי* (4 השלבים הממוספרים שכבר קיימים ב-`#process`).
**עיצוב:** לא נוצר shape חדש — המקטע משתמש באותו דפוס grid בדיוק כמו `.process__item` (עמודה ראשונה קבועה + טקסט), רק עם המילה עצמה (לא מספר) בעמודה הראשונה, בצבע ובגופן sage-deep/serif שכבר קיימים בכל האתר. שום קו דקורטיבי, שום Timeline, שום ספרייה חדשה.
**נבדק:** דסקטופ, מובייל, RTL, שאין חפיפה עם `#process` שאחריו, ושה-Hero, `/fit`, וכל שאר הקישורים לא נפגעו.
**לא נגעתי:** בניווט העליון (לא נוסף קישור למקטע החדש) — הוא מתגלה בגלילה טבעית, לא עוד פריט תפריט.

---

## 2026-08-02 — דף חדש: `/research`, מחקר אנונימי (לא עוד שאלון סינון)

### רקע — למה זה נפרד מ-`/fit` ולא עוד שינוי בתוכו
לפני שנכנסנו לקוד דנו בזה כ-Product: `/fit` הוא שאלון **סינון והתאמה** — יש בו ניקוד, יש בו תוצאה (A/B/C), והוא תמיד מוביל לוואטסאפ/Calendly. מה שביקשת הפעם הוא משהו אחר לגמרי מבחינת מטרה: **מחקר קהל אנונימי**, בלי לחץ להמיר, שהמטרה שלו היא בעיקר ללמוד את השפה והכאבים האמיתיים של הקהל (ורק בשוליים, ובלי חובה, לפתוח דלת ליצירת קשר). ניסיון "לדחוס" את זה לתוך `/fit` היה פוגע בשניהם — גם במהימנות של `/fit` כשאלון קצר וממוקד-המרה, וגם במהימנות של המחקר כאנונימי-לא-מוכר. לכן זה דף עצמאי, `research/`, לא תוספת שאלות ל-`fit/`.

### 25. דף חדש `research/index.html` + `research/research.css` + `research/research.js`
**מה נוסף:** שאלון בן 5 שאלות (בחירת שלב-חיים → סולם דירוג 1-5 → מסך רפלקציה → שאלה פתוחה אחת → בחירה מרובה → בחירה בודדת) ומסך סיום עם שיקוף רגשי + שדה יצירת קשר אופציונלי (שם + טלפון/אימייל), בלי חובה למלא.
**איך נבנה:** לא הומצא מנוע חדש — `research.js` הוא שכפול-והתאמה של אותה state machine מ-`fit.js` (ניווט בין `data-step`, שמירה זמנית ב-`sessionStorage`, מסך רפלקציה עם passthrough, ולידציה לפני "המשך"). ההבדל המהותי היחיד בלוגיקה: **אין ניקוד ואין תוצאה מסועפת** — כל מי שמסיים מגיע לאותו מסך תודה, בלי "דירוג התאמה".
**עיצוב:** משתמש ישירות ב-`fit__card`, `fit__option`, `fit__nav`, `fit__btn-continue` וכו' מ-`fit.css` — לא שוכפל CSS קיים. `research.css` מוסיף רק שלושה רכיבים שלא היו קיימים באתר: סולם דירוג (`research__scale`), עיצוב ל-textarea (לא היה קיים כלל, ל-Q3 הישן ב-`fit` הוסבה לבחירה מרובה בסבב 14 והוסר ה-CSS שלה אז — כאן זה חזר, כי דווקא כאן, במחקר לא-ממיר, שאלה פתוחה היא בדיוק הלב של המטרה ולא סיכון נשירה שצריך למנוע), ופאנל ה-opt-in.
**עיקרון מרכזי ששמרתי עליו מהדיון שלנו:** השאלה הפתוחה **באמצע** (Q3 מתוך 5), לא בהתחלה (יותר מדי חיכוך מוקדם) ולא בסוף (הכי חשוב, לא כדאי לסכן אותה כשהמשתמש כבר "עייף").

### 26. שדה יצירת קשר — עיצוב מכוון ל"לא-חובה"
**מה השתנה:** בניגוד לכל שדה חובה אחר בשאלון, `optinName` ו-`optinContact` לא נבדקים ב-validation בכלל — כפתור "לסיום ושליחה" תמיד פעיל, גם ריק.
**למה:** זה בדיוק ההבדל בין "מחקר עם ניסיון סמוי למכור" לבין מחקר אמיתי. אם הכפתור היה נחסם עד מילוי השדות, כל האתוס של "אנונימי, בלי מכירה" היה קורס ברגע האחרון.

### 27. שכבת שליחה + גיבוי מקומי (`research.js`) + `research/SETUP-RESEARCH.md`
**מה נוסף:** פונקציית `submitPayload()` ששולחת `fetch POST` ל-`SUBMIT_ENDPOINT` (קבוע ריק כרגע בראש הקובץ), ונופלת בעדינות ל-`localStorage` (`baderech_research_queue`) אם אין endpoint מוגדר או שהקריאה נכשלת.
**למה זה נחוץ בכלל:** האתר, כמו שכתוב למעלה בקובץ הזה וב-README, **סטטי לגמרי — אין בקאנד, אין DB**. `/fit` לא היה צריך את זה כי הוא שולח את כל ה"תוצאה" כהודעת WhatsApp למכשיר של המבקר עצמו. אבל מחקר אנונימי לא יכול לעבוד ככה — אם כל תשובה "נעלמת" למכשיר של העונה ולא מגיעה אליך, אין מחקר. בחרתי בפתרון הכי קרוב לרוח הפרויקט (אין build tool, אין שרת): Google Apps Script Web App שכותב לגיליון Sheets, בחינם. כתבתי הוראות מלאות + קוד מוכן להדבקה ב-`SETUP-RESEARCH.md` — **זה עדיין לא מחובר בפועל**, כי זה דורש שאתה תיצור את הגיליון ואת החשבון שמריץ אותו, לא משהו שאני יכול לעשות בשבילך.
**⚠️ פעולה שנשארת אצלך:** בלי לבצע את 5 השלבים ב-`SETUP-RESEARCH.md`, כל תשובה במחקר נשמרת רק מקומית בדפדפן של כל עונה בנפרד ולא מגיעה אליך בכלל. זו לא בעיה בקוד — זו בכוונה, כי לא רציתי ליצור חשבון Google/גיליון בשמך בלי שתאשר.

### מה עוד עלה בדיון ולא הכרעתי לבד — צריך החלטה שלך
- **הברקוד:** כרגע ה-QR הקיים (`qr/baderech-fit-qr.png`) עדיין מפנה ל-`/` (דף הבית), לא ל-`/research`. לא שיניתי את זה — זו החלטת הפצה (איזה ברקוד יוצג איפה, האם יהיה ברקוד נפרד למחקר מול ברקוד ל-`/fit`), לא החלטת קוד.
- **הטקסט ב-`closingReflection`** (מסך הסיום) הוא כרגע ניחוש סביר שלי, לא מבוסס על תשובות אמיתיות — כי עוד אין תשובות. ברגע שיצטברו כמה עשרות שורות בגיליון, שווה לחזור ולעדכן את המשפט הזה כך שבאמת ישקף מה שאנשים כותבים, לא מה שאני מניח שהם יכתבו.

---

## 2026-08-02 (המשך) — יומן ההרצה החיה: תקלות פריסה, תיקוני קוד, וליטוש אחרי לידים אמיתיים

הסבב הזה שונה מקודמיו — הוא לא תכנון מראש, אלא תיעוד של מה שקרה בפועל ברגע שהעמוד יצא לאוויר וניסית אותו על אנשים אמיתיים. חלק מהתקלות היו טעויות פריסה (לא קוד), חלק היו באגים אמיתיים, וחלק היו החלטות מוצר על סמך מה שהתברר בשטח.

### תקלות פריסה (לא קוד — תיעוד למקרה שיקרה שוב)
28. **`research/index.html` דרס את `index.html` הראשי בשורש.** כשההעלאה הראשונית נעשתה דרך "Add files via upload" בגיטהאב בלי לציין נתיב תיקייה, הקובץ נחת בשורש במקום בתוך `research/` ודרס את דף הבית האמיתי. תוקן ידנית על ידך (שחזור מההיסטוריה של git). **לקח למחשבה עתידית:** בהעלאת קבצים חדשים דרך ה-UI של גיטהאב, תמיד להשתמש ב-Create new file ולהקליד את הנתיב המלא (`research/שם-קובץ`) בשדה השם, לא Upload files סתמי.
29. **`SUBMIT_ENDPOINT` הודבק בתוך הערת קוד (`//`) במקום כערך משתנה אמיתי**, ואז בפעם השנייה הודבק בלי מירכאות ובלי נקודה-פסיק — מה ששבר את כל קובץ ה-`research.js` (שגיאת syntax, כל הכפתורים הפסיקו להגיב). שני המקרים תוקנו ידנית.
30. **הרשאות ה-Apps Script היו "Only myself" במקום "Anyone".** זו הסיבה שהטופס "עבד" (הגיע למסך תודה) אבל אף שורה לא נכתבה לגיליון — גוגל חסם את הבקשה בשקט כי היא הגיעה ממבקר אנונימי בלי חשבון מחובר. תוקן דרך Deploy → Manage deployments → Who has access → Anyone, עם דפלוימנט חדש מאפס (גיליון + פרויקט Apps Script חדשים) כדי לצאת עם דף נקי.
31. **תקלת cache בדפדפן** גרמה לזמן מה לכל שלבי השאלון להיראות מוצגים בבת אחת, בלי הפרדה — נראה כמו באג קוד חמור, אך התברר (בבדיקת גלישה פרטית) שזו הייתה גרסה ישנה תקועה בדפדפן, לא בעיה באתר עצמו.

### תיקוני קוד אמיתיים

32. **מילון תרגום לעברית (`LABELS`) ב-`research.js`.** הגיליון קיבל קודים טכניים (`q1_single`, `q5_none`) במקום טקסט קריא. נוסף מילון (בדומה למה שכבר קיים ב-`fit.js`) שמתרגם את הקוד לתשובה בעברית לפני השליחה ל-Sheet. Q2 (דירוג מספרי) ו-Q3 (טקסט חופשי) לא נזקקו לתרגום — הם כבר קריאים כמו שהם.
33. **שדה הטלפון (`optinContact`) קיבל `dir="ltr"` ו-`inputmode="tel"`.** בעמוד RTL, הקלדת ספרות בשדה בלי כיוון מפורש עלולה להיכנס בסדר לא-צפוי. בנוסף, קוד ה-`doPost` ב-Apps Script עודכן לכתוב לעמודת הטלפון בפורמט טקסט (`setNumberFormat('@')`) *לפני* הכתיבה בפועל, כדי ש-Sheets לא ימיר מספרים עם 0 מוביל למספר "אמיתי" ויאבד את ה-0.
34. **מסך "עוד רגע, כמעט סיימנו..." (`sendingHint`)** נוסף למסך הסיום — מופיע רק אם השליחה לוקחת יותר מ-2.2 שניות (Cold Start אמיתי של Apps Script), כדי שהמתנה תרגיש כמו תהליך ולא כמו תקיעה. אם השליחה מהירה, המשתמש אף פעם לא רואה את זה.

### שינויי ניסוח וחוויה — על סמך שימוש אמיתי, לא ניחוש

35. **הוסרה המילה "מכירה" מכל מקום בעמוד** (תווית ה-opt-in, meta description, שתי גרסאות של הודעת התודה). הנימוק שלך: לכתוב "בלי מכירה" בעצם *מזכיר* מכירה ומפעיל בדיוק את המסגור שרצינו להימנע ממנו ("אל תחשוב על פיל"). הביטוי הוחלף בניסוח שמשיג את אותה הרגעה בלי לצטט את המילה עצמה (למשל "סתם שיחה" במקום "בלי מכירה, סתם שיחה").
36. **הוסר הטקסט "להמשיך" ממסך הרפלקציה.** כל המסך כבר היה לחיץ-בכל-מקום מלכתחילה; ההנחיה הטקסטואלית נועדה במקור כרמז ויזואלי (כי אין כפתור נראה-לעין), אבל אחרי שימוש אמיתי התברר שהיא נשמעת כמו כפתור נפרד ומבלבלת יותר משהיא עוזרת. הלחיצה על כל האזור ממשיכה לעבוד בדיוק כמו קודם — רק ההסבר הוסר.
37. **מסך הרפלקציה כולו הוסר מ-`/research`** (Q2 עובר ישירות ל-Q3). ההיגיון המקורי — רגע נשימה לפני השאלה הכי אישית — נשאר תקף עקרונית, אבל **החלטתך** הייתה שברגע שיש כבר כפתור "המשך" ברור בכל שאר השלבים, המסך הזה מרגיש מיותר יותר משהוא עוזר, במיוחד בלי שום נתון עדיין שמראה שהוא מונע נשירה. קוד ה-`data-passthrough` ב-`research.js` נשאר בקובץ בלי שימוש (לא מזיק, סתם קוד רדום) — לא נוקה, כדי לא לגעת בקובץ מעבר לנדרש.

### QR — שינוי יעד

38. **ה-QR הראשי עודכן מ-`/` (דף הבית) ל-`/research?src=qr-card`.** ההחלטה: מכיוון שעדיין לא הודפס/חולק שום כרטיס פיזי, אין קונפליקט עם מה שכבר בשטח — אז נבחרה האפשרות הפשוטה (לשנות את הקיים, לא ליצור QR שני). ההיגיון: `/research` הוא נקודת הכניסה הכי נמוכת-מחויבות שיש באתר, ולכן הכי מתאימה לחשיפה ראשונה קרה דרך QR פיזי — בדיוק המקרה שה-QR נועד לו. אם בעתיד יתווספו ערוצי הפצה עם רמת חמימות שונה (למשל הפניה אישית שממילא לא צריכה QR, מול שולחן קר בבית קפה), אולי יהיה שווה לשקול QR שני נפרד ל-`/fit` או לדף הבית — אך זה לא נעשה כרגע.
39. **`qr/baderech-fit-qr.png` ו-`.svg` נוצרו מחדש** עם הקידוד החדש, ואומתו בפועל (סריקה/פענוח, לא רק "נראה כמו QR"). `README.md` עודכן בהתאם (מסע המשתמש, מבנה הפרויקט, סעיף הברקוד). **טרם טופל:** קובצי ההדפסה `card-a6-105x148mm.png` ו-`card-business-85x55mm.png` עדיין מכילים כנראה את ה-QR הישן משובץ בעיצוב — לא עודכנו, כי אין גישה לקובצי המקור/עיצוב שלהם. אם תעלה אותם, אפשר לבנות אותם מחדש עם ה-QR העדכני.

---
